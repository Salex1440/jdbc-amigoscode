app:
  datasource:
    main:
      driver-class-name: org.postgresql.Driver
      jdbc-url: jdbc:postgresql://localhost:5432/salex
      username: postgres
      password: 'postgres'
      pool-size: 30

server:
  error:
    include-binding-errors: always
    include-message: always


